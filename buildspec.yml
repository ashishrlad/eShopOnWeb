version: 0.2    
phases:
  install:
    commands:
      - curl -sSL https://dot.net/v1/dotnet-install.sh | bash /dev/stdin --channel STS 
  build:
    commands:
      - dotnet restore Net7eShopOnWeb/Net7eShopOnWeb.sln
      - dotnet build  Net7eShopOnWeb/Net7eShopOnWeb.sln
